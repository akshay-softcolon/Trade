export const hasAccess = moduleName => {
//   if (moduleName === undefined) { throw new Error('Required role needs to be set') }

//   return compose().use((req, res, next) => {
//     if (req.role.modules.indexOf(moduleName) != -1) {
//       next()
//     } else {
//       return response.sendError(res, messages.unauthorized_module)
//     }
//   })
}
